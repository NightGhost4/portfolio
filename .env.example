# Production origin, used to build absolute Open Graph, canonical, and sitemap URLs.
# If unset, these fall back to http://localhost:3000 (fine for local dev).
# Set this in your hosting provider's environment variables for production.
NEXT_PUBLIC_SITE_URL=https://tylernorcross.com
